# Processed Data

Final train/test splits ready for model training. Not committed.

| File | Description |
|------|-------------|
| `X_train.parquet` | Training features (80% stratified split) |
| `X_test.parquet` | Test features (20% stratified split) |
| `y_train.parquet` | Training labels |
| `y_test.parquet` | Test labels |

Run `make data` to regenerate.
